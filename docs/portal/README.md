# GANSU2 User Portal (SPA)

このディレクトリは `secret/aws/lambda_portal/frontend/` の複製である。
**正本はそちら**で、ここは GitHub Pages から配るためのコピー。

`config.js` だけは Pages 用に別内容になっている（API_BASE を明示する）。
SPA 本体を変更したときは frontend/ 側を直し、config.js 以外をここへ複製すること。

Jekyll のレイアウトは当てない（docs/_config.yml の exclude / defaults を参照）。
